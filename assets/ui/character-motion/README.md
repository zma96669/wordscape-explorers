# 探险员基础动作

打开 `preview.html` 可查看待机、移动、思考、开心、鼓励五种状态。

当前版本只使用 `assets/art/characters/explorer.png`，以平移、缩放和旋转实现整图动效。它保持统一尺寸、底部锚点和角色身份，但不包含真实眨眼、挥手、手脚交替或面部变化。要实现这些动作，需要提供分层角色部件或同一角色的逐帧图。

`actions.json` 记录动作ID、时长、循环属性和实现方式。样式支持系统 `prefers-reduced-motion`，预览页也可以手动关闭动画。
